[project]
name = "y0mu"
version = "0.2.0"
description = "Конвертер манги с mangalib.me под Kindle 11th gen"
requires-python = ">=3.13,<3.14"
dependencies = [
    "httpx>=0.27",
    "pillow>=10.3",
    "keyring>=25.0",
    "pyside6-essentials>=6.8",
]

[project.scripts]
y0mu = "y0mu.cli:main"
y0mu-gui = "y0mu.gui.app:main"

[dependency-groups]
dev = ["pytest>=8.0", "pyinstaller>=6.10"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/y0mu"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
